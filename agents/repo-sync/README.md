# Repo Sync

# Agent scaffold to detect CI/deployment issues and propose PR-based fixes.

# Includes:

- `agent.json` — identity and metadata
- `system_prompt.md` — agent personality and rules
- `skills/` — actionable skill files
- `tools/tool_config.json` — allowed tools
- `memory/` — long-term and handoff notes