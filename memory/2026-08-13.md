# Memory — 2026-08-13

## Big Install Session — Tools & Repos

Full day of installing the AI stack into workspace-main.

### Repos Cloned (repos/)
- mem0 — agent memory layer (Qdrant + Ollama)
- litellm — token counting + LLM gateway
- firecrawl — JS-heavy web scraper
- newspaper — lightweight article scraper
- graphify — knowledge graph for codebase/docs
- langgraph — stateful multi-step agent orchestration
- zep — agent memory SDK
- paul — Plan-Apply-Unify Loop for Claude Code dev
- crewAI — multi-agent crews
- openai-agents-python — OpenAI Agents SDK
- aider — AI pair programmer CLI
- DB-GPT — AI data assistant (SQL, code, analysis)
- Flowise — visual LLM flow builder (PAUSED — OOM during npm install)

### Python Envs Created
- `.venv` (Python 3.14) — mem0ai, litellm, firecrawl-py, newspaper3k, graphifyy, langgraph, zep-cloud, openai-agents
- `.venv-crew` (Python 3.11) — crewai 1.15.16, aider-chat 0.86.2
  - crewAI blocked on Python 3.14 (pydantic v1 incompatible), fixed with separate 3.11 venv

### Tool Scripts Created (tools/)
- scrape.py — newspaper first, firecrawl fallback
- memory_store.py — store/search per-agent memories via mem0
- token_tracker.py — count tokens, estimate LLM costs

### Services Confirmed Running
- Open WebUI → localhost:8080 (v0.8.12) ✅
- DB-GPT → localhost:5670 (mistral + nous-hermes2 + nomic-embed-text via Ollama) ✅
- Ollama → all 6 models running ✅

### Other
- Installed Rust (needed for tiktoken build)
- graphify OpenClaw skill installed at ~/.openclaw/skills/graphify/
- Node 22 LTS installed via brew (needed for Flowise)
- Flowise npm install SIGKILL'd on Node 22 (OOM) — on todo list
- tools/crew_starter.py created — Researcher + Writer crew runs on Ollama/mistral
- COMMAND_LIST.md created as master status + todo sheet

### Disk at end of session
- Free: ~60 GB
- workspace-main: 4.9 GB
- ~/.openclaw: 7.4 GB
