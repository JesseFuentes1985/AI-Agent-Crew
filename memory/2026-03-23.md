# Memory — 2026-03-23

## Homebrew / CLT Fix (Jesse's Mac mini)
- Jesse had a Homebrew error: Command Line Tools out of date, blocking `brew install gifgrep`
- Steps taken:
  1. `sudo rm -rf /Library/Developer/CommandLineTools` — removed old CLT
  2. `xcode-select --install` — triggered install request
  3. Popup was suppressed; used `softwareupdate --list` instead
  4. Found: "Command Line Tools for Xcode 26.3-26.3" (857MB)
  5. Installed via: `softwareupdate --install "Command Line Tools for Xcode 26.3-26.3" --agree-to-license`
  6. After install: `brew install gifgrep` succeeded — gifgrep 0.2.1 installed ✅
- Jesse accidentally pasted reply text into terminal at one point (bash complained "command not found: My") — recovered fine
- Jesse was in bash (bash-3.2$) during the process, used `exit` to return to zsh

## OpenClaw TUI Notes
- Jesse wasn't familiar with how to open/exit the OpenClaw TUI
- TUI command: `openclaw tui`
- Exit TUI: press `Ctrl+C` twice (second press shown in UI as "press ctrl+c again to exit")
- Running commands like `openclaw doctor --non-interactive` requires exiting TUI first

## Gateway
- Gateway restart update ok (npm) at 19:14 PDT
- `openclaw doctor --non-interactive` was triggered by system around that time

## GitHub / AI Agent Crew Dashboard
- Repo: https://github.com/JesseFuentes1985/AI-Agent-Crew
- Live site: https://jessefuentes1985.github.io/AI-Agent-Crew/
- Local path: /Users/jessefuentes/.openclaw/workspace-main (this IS the repo)
- Built as a single index.html SPA (no framework, vanilla JS)
- Avatars stored in: dashboard/avatars/
- Avatar map in JS: AVATARS object (line ~281 in dashboard/index.html)
- Agent detail page uses .agent-hero-avatar class, crew grid uses .agent-avatar
- TODO: Add orbit-spin.gif for Orbit's detail page only (DETAIL_AVATARS map)
