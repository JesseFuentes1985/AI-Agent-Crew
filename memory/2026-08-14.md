# Memory — 2026-08-14

## Session Notes

### Error banner at top of UI
- Was a ghost from a previous session — exec command used natural language with `->` separators instead of shell syntax
- DB-GPT venv confirmed healthy: .pth files exist, dbgpt + dbgpt-sandbox binaries in bin/

### COMMAND_LIST.md updated
- Added Agent Workspace Files section — per-agent file inventory, what each workspace has
- Updated date, added changelog entry

### MEMORY.md rebuilt
- Context window frustration — Jesse pointed out that MEMORY.md was slimmed too aggressively on 2026-08-09 (77% token reduction went too far)
- Lost: Last Star / Hex ship detail, model routing strategy, Open WebUI LaunchAgent, Notion MCP / Blender MCP todos, full agent skills
- Fixed: merged MEMORY-backup-2026-08-09.md back in + all August 13 session context
- Also wrote missing 2026-08-13.md daily note (that session was never logged)

### Key insight logged
- memory_search is still broken (local GGUF embedding provider — needs `openclaw plugins install @openclaw/llama-cpp-provider`)
- When memory_search is down, must read files directly to recover context
- Must always write daily notes AND keep MEMORY.md fat enough to be useful on its own
