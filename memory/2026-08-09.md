# Memory — 2026-08-09

## Session — ~2:40–3:00 PM PDT (RAG System Setup)

Jesse checked in after ~3.5 months away (last session was April 18).

### System health at check-in
- Mac mini uptime: 40 days ✅
- Disk: 17GB / 228GB (18%) ✅  
- Ollama: all 6 models running ✅
- Open WebUI: http://localhost:8080 ✅
- OpenClaw: v2026.7.1-2, 8 agents registered ✅
- One minor config warning: Ollama plugin disabled but config entries present (non-breaking)

### Priority: RAG System for Token Savings

Jesse's #1 priority: set up a RAG (Retrieval-Augmented Generation) system in a local DB
to reduce token costs while staying efficient.

### What We Built

**Stack:**
- ChromaDB 1.5.9 (persistent local vector store)
- nomic-embed-text via Ollama (768-dim embeddings, free, local)
- Python 3.11 venv (not 3.14 — too new for most packages)

**Location:** `~/.openclaw/rag/`

**CLI:** `rag "query"` | `rag ingest` | `rag stats`

**Ingested:**
- 148 files → 166 vectors across all 8 agent workspaces
- All .md files from workspace-main, workspace-baymax, workspace-beast,
  workspace-greenlantern, workspace-quigon, workspace-rick, workspace-thanos, workspace-tonystark

**MEMORY.md slimmed:**
- Before: 161 lines / 6,447 bytes
- After: 49 lines / 1,492 bytes
- **77% token reduction** on the biggest injected context file
- Full detail preserved in RAG DB

**Cron job:** `rag-daily-sync` — runs `rag ingest` at 4 AM daily via Mistral model (free)

### How to Use RAG
```bash
rag "what are Jesse's pending todos"
rag "Ollama models installed"
rag "Last Star project" --agent greenlantern
rag ingest --verbose        # re-index after file changes
rag stats                   # show vector counts
```

### Outstanding / Next Steps for RAG
- [ ] Wire nomic-embed-text into OpenClaw memory config (replace OpenAI embeddings)
- [ ] Build MCP server wrapper so all agents query RAG via mcporter (not just exec)
- [ ] Add structured knowledge: agent tasks, project status, calendar context
- [ ] Auto-ingest trigger: watch for file changes and re-index immediately
- [ ] Slim AGENTS.md (214 lines / 8.1 KB — next biggest token cost)
